
# CS554 final

This project is managed by `pnpm`.

To install (from root)
---

`pnpm install -r`

To build (from root)
---

`pnpm recursive run build`

To debug (from root)
---

`pnpm recursive run debug`
